import {connectionWrapperType} from '../../types/wrapper.type';

/**
 *
 * @method Or
 * @param {unknown} argument
 * @param {((...arg: unknown[]) => boolean)[]} targetApplyList
 * @param {unknown[]} secondArgumentList
 */
export declare const OrWrapper: connectionWrapperType;
